name: Github Actions Demo

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
    branches:
      - "*feature"

jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World"
